# /etc/systemd/system/powersave.service
[Unit]
Description=Set CPU governor to powersave

[Service]
Type=oneshot
ExecStart=/bin/sh -c 'for cpu in /sys/devices/system/cpu/cpu[0-9]*; do echo powersave > $cpu/cpufreq/scaling_governor; done'

[Install]
WantedBy=multi-user.target
