This is subdirectory performs the target detection and localisaton.

It has a debugging QT application to see if the target detection works.
It provides a callback to bratmobile with its coordinates.

